{
  "mcpServers": {
    "mongodb": {
      "command": "npx",
      "args": [
        "-y",
        "@nam088/mcp-mongodb"
      ],
      "env": {
        "MONGODB_URI": "mongodb://admin:admin@localhost:27017/db_name?authMechanism=SCRAM-SHA-256&directConnection=true",
        "MONGODB_DATABASE": "idlogiq",
        "MONGODB_MODE": "READONLY"
      }
    }
  }
}

